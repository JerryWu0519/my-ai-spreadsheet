import { BanksheetLogoType } from '@/shared/components/BanksheetLogo';

/**
 * QuadraticLogoType — now renders BanksheetLogoType.
 * Preserves the export name so all existing imports continue to work.
 */
export const QuadraticLogoType = () => <BanksheetLogoType className="text-foreground" />;
